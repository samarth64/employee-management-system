services:
  - type: web
    name: eemployee-management-system
    runtime: python
    buildCommand: ""
    startCommand: "gunicorn app:app"
    envVars:
      - key: FLASK_ENV
        value: production
    region: oregon
